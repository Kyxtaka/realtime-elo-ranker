/* eslint-disable prettier/prettier */
import { rankingModel } from './ranking.model';

describe('RankingModel', () => {
  it('should be defined', () => {
    expect(rankingModel).toBeDefined();
  });

  // Add specific tests if RankingModel has methods
});
